[project]
name = "dir-example"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
    "agtncy-dir",
]

[[tool.uv.index]]
url = "https://buf.build/gen/python"

[tool.uv.sources]
agtncy-dir = { path = "../", editable = true }
