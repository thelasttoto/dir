{{- if eq .Values.spire.enabled true }}
{{- range .Values.spire.federation }}
---
apiVersion: spire.spiffe.io/v1alpha1
kind: ClusterFederatedTrustDomain
metadata:
  name: {{ include "chart.fullname" $ }}-{{ .trustDomain | replace "." "-" }}
spec:
  {{ . | toYaml | nindent 2 }}
{{- end }}
{{- end }}
