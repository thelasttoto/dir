global:
  spire:
    trustDomain: "test.example.com"
    clusterName: "test-cluster"

server:
  persistence:
    enabled: false
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

agent:
  resources:
    limits:
      cpu: 100m
      memory: 128Mi
    requests:
      cpu: 50m
      memory: 64Mi

rbac:
  create: true

namespace:
  create: true
  name: "test-spire"
